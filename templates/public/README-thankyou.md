# صفحه تشکر بعد از خرید - راهنمای ویرایش

## 📁 فایل‌های مرتبط

### 1. فایل Template (HTML/PHP)
**مسیر:** `templates/public/order-received.php`

این فایل اصلی template صفحه تشکر است که می‌توانید آن را ویرایش کنید.

**قسمت‌های قابل ویرایش:**
- هدر تشکر (خطوط 42-58)
- کارت‌های اطلاعات سفارش (خطوط 61-93)
- بخش جزئیات سفارش (خطوط 118-126)
- دکمه‌های عملیات (خطوط 128-136)

**نکات مهم:**
- از `esc_html()` و `esc_url()` برای امنیت استفاده کنید
- برای تغییرات در HTML، این فایل را ویرایش کنید
- برای تغییرات در استایل، فایل CSS را ویرایش کنید

---

### 2. فایل کنترلر (PHP Logic)
**مسیر:** `public/woocommerce-thankyou.php`

این فایل منطق override کردن template را مدیریت می‌کند.

**قسمت‌های قابل ویرایش:**
- تابع `sc_override_thankyou_template()` - برای تغییر مسیر template
- تابع `sc_custom_thankyou_content()` - برای اضافه کردن محتوای سفارشی

**نکات مهم:**
- اگر می‌خواهید template دیگری استفاده کنید، مسیر را در خط 22 تغییر دهید
- برای اضافه کردن hook‌های جدید، از `add_action()` استفاده کنید

---

### 3. فایل استایل (CSS)
**مسیر:** `assets/css/public.css`

تمام استایل‌های صفحه تشکر در این فایل قرار دارند.

**قسمت‌های قابل ویرایش:**
- استایل‌های صفحه تشکر از خط 1613 شروع می‌شوند
- کامنت `/* ============================================` را جستجو کنید

**کلاس‌های اصلی:**
- `.sc-thankyou-page` - کانتینر اصلی
- `.sc-thankyou-header` - هدر تشکر
- `.sc-thankyou-cards` - کارت‌های اطلاعات
- `.sc-thankyou-card` - هر کارت
- `.sc-thankyou-order-details` - بخش جزئیات
- `.sc-thankyou-actions` - دکمه‌های عملیات

**نکات مهم:**
- برای تغییر رنگ‌ها، مقدار `#6D34FF` را جستجو کنید
- برای تغییر فاصله‌ها، `padding` و `margin` را تغییر دهید
- برای تغییر اندازه فونت، `font-size` را تغییر دهید

---

## 🎨 تغییرات پیشنهادی

### تغییر رنگ اصلی
در فایل `assets/css/public.css`، تمام `#6D34FF` را با رنگ مورد نظر خود جایگزین کنید.

### اضافه کردن کارت جدید
در فایل `templates/public/order-received.php`، یک `<div class="sc-thankyou-card">` جدید اضافه کنید.

### تغییر متن‌ها
در فایل `templates/public/order-received.php`، متن‌های فارسی را تغییر دهید.

---

## 🔧 عیب‌یابی

### صفحه تشکر نمایش داده نمی‌شود
1. بررسی کنید که فایل `public/woocommerce-thankyou.php` در `sportclub_manager.php` include شده باشد
2. بررسی کنید که فایل `templates/public/order-received.php` وجود دارد
3. Cache مرورگر را پاک کنید

### استایل‌ها اعمال نمی‌شوند
1. بررسی کنید که فایل `assets/css/public.css` به درستی لود می‌شود
2. Cache مرورگر را پاک کنید
3. بررسی کنید که کلاس‌های CSS درست استفاده شده‌اند

---

## 📝 مثال تغییرات

### مثال 1: تغییر رنگ اصلی به قرمز
در `assets/css/public.css`:
```css
/* جستجو و جایگزینی */
#6D34FF → #d63638
#5a2ae6 → #b32d2e
```

### مثال 2: اضافه کردن کارت جدید
در `templates/public/order-received.php`:
```php
<div class="sc-thankyou-card sc-thankyou-card-custom">
    <div class="sc-thankyou-card-icon">🎁</div>
    <div class="sc-thankyou-card-content">
        <div class="sc-thankyou-card-label">متن برچسب</div>
        <div class="sc-thankyou-card-value">مقدار</div>
    </div>
</div>
```

---

## 📞 پشتیبانی

اگر سوالی دارید یا مشکلی پیش آمد، فایل‌های زیر را بررسی کنید:
- `templates/public/order-received.php` - Template
- `public/woocommerce-thankyou.php` - Logic
- `assets/css/public.css` - Styles

