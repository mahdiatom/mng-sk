# راهنمای نصب PhpSpreadsheet برای خروجی Excel

## روش 1: استفاده از Composer (پیشنهادی)

### مرحله 1: نصب Composer

#### در Windows:

1. فایل `Composer-Setup.exe` را از [getcomposer.org/download](https://getcomposer.org/download/) دانلود کنید
2. فایل را اجرا کنید و مراحل نصب را دنبال کنید
3. Composer به صورت خودکار به PATH اضافه می‌شود

#### بررسی نصب Composer:

در Command Prompt یا PowerShell دستور زیر را اجرا کنید:
```bash
composer --version
```

اگر نسخه Composer نمایش داده شد، نصب موفق بوده است.

### مرحله 2: نصب PhpSpreadsheet

1. در Command Prompt یا PowerShell به پوشه افزونه بروید:
   ```bash
   cd "C:\xampp\htdocs\ai.com\wp-content\plugins\AI sportclub"
   ```

2. دستور زیر را اجرا کنید:
   ```bash
   composer install
   ```

3. پس از اتمام، پوشه `vendor` ایجاد می‌شود و PhpSpreadsheet نصب می‌شود.

---

## روش 2: استفاده از Composer.phar (بدون نصب Composer)

اگر نمی‌خواهید Composer را نصب کنید:

1. فایل `composer.phar` را از [getcomposer.org/download](https://getcomposer.org/download/) دانلود کنید
2. فایل را در پوشه افزونه قرار دهید
3. در Command Prompt به پوشه افزونه بروید و دستور زیر را اجرا کنید:
   ```bash
   php composer.phar install
   ```

---

## روش 3: دانلود دستی (اگر روش‌های بالا کار نکرد)

اگر هیچکدام از روش‌های بالا کار نکرد، می‌توانید PhpSpreadsheet را به صورت دستی دانلود کنید:

1. از [GitHub Releases](https://github.com/PHPOffice/PhpSpreadsheet/releases) آخرین نسخه را دانلود کنید
2. فایل ZIP را استخراج کنید
3. محتویات پوشه `src` را به `vendor/phpoffice/phpspreadsheet/src` کپی کنید
4. فایل `autoload.php` را از این repository کپی کنید

---

## بررسی نصب موفق

پس از نصب، فایل زیر باید وجود داشته باشد:
```
vendor/phpoffice/phpspreadsheet/src/PhpSpreadsheet/Spreadsheet.php
```

اگر این فایل وجود دارد، نصب موفق بوده است و می‌توانید از دکمه "📊 خروجی Excel" استفاده کنید.

---

## مشکل دارید؟

اگر با مشکلی مواجه شدید:
1. مطمئن شوید PHP extension `zip` فعال است
2. مطمئن شوید دسترسی نوشتن در پوشه افزونه را دارید
3. لاگ خطاهای PHP را بررسی کنید














