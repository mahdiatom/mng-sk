# راهنمای نصب PhpSpreadsheet برای خروجی Excel

برای استفاده از قابلیت خروجی Excel، باید کتابخانه PhpSpreadsheet را نصب کنید.

## مراحل نصب:

1. در پوشه افزونه (`AI sportclub`)، فایل `composer.json` ایجاد شده است.

2. در خط فرمان (Terminal/Command Prompt)، به پوشه افزونه بروید:
   ```bash
   cd "C:\xampp\htdocs\ai.com\wp-content\plugins\AI sportclub"
   ```

3. دستور زیر را اجرا کنید:
   ```bash
   composer install
   ```

4. اگر Composer نصب نیست، ابتدا آن را از [getcomposer.org](https://getcomposer.org/) دانلود و نصب کنید.

## پس از نصب:

پس از نصب موفق، می‌توانید از دکمه "📊 خروجی Excel" در صفحات زیر استفاده کنید:
- لیست صورت حساب‌ها
- لیست حضور و غیاب
- لیست اعضا

## نکات:

- فایل Excel با فرمت `.xlsx` ایجاد می‌شود
- نام فایل شامل اطلاعات فیلترهای اعمال شده است
- فایل Excel شامل تمام ستون‌های نمایش داده شده در لیست است
- در لیست اعضا، دوره‌های هر کاربر در ستون جداگانه نمایش داده می‌شود

